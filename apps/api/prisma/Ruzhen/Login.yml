info:
    name: Login
    type: http
    seq: 2

http:
    method: POST
    url: http://localhost:4000/api/auth/login
    auth: inherit

settings:
    encodeUrl: true
    timeout: 0
    followRedirects: true
    maxRedirects: 5
