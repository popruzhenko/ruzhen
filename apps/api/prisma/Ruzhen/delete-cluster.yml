info:
    name: delete-cluster
    type: http
    seq: 10

http:
    method: DELETE
    url: http://localhost:4000/api/admin/clusters
    auth: inherit

settings:
    encodeUrl: true
    timeout: 0
    followRedirects: true
    maxRedirects: 5
