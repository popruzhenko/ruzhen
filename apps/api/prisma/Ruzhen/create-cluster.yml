info:
    name: create-cluster
    type: http
    seq: 6

http:
    method: POST
    url: http://localhost:4000/api/admin/clusters
    auth: inherit

settings:
    encodeUrl: true
    timeout: 0
    followRedirects: true
    maxRedirects: 5
