info:
    name: cluster-block
    type: http
    seq: 11

http:
    method: POST
    url: http://localhost:4000/api/admin/clusters/
    auth: inherit

settings:
    encodeUrl: true
    timeout: 0
    followRedirects: true
    maxRedirects: 5
