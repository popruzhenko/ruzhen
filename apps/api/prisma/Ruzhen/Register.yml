info:
    name: Register
    type: http
    seq: 1

http:
    method: POST
    url: auth/register
    auth: inherit

settings:
    encodeUrl: true
    timeout: 0
    followRedirects: true
    maxRedirects: 5
